'use strict';

describe('Directive: esdmsTagging', function () {
  beforeEach(module('esDmsSiteApp'));

  /*var element;

  it('should make hidden element visible', inject(function ($rootScope, $compile) {
    element = angular.element('<esdms-tagging></esdms-tagging>');
    element = $compile(element)($rootScope);
    expect(element.text()).toBe('this is the esdmsTagging directive');
  }));*/
});
