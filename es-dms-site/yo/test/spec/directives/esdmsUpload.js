'use strict';

describe('Directive: esdmsUpload', function () {
  beforeEach(module('esDmsSiteApp'));

  /*var element;

  it('should make hidden element visible', inject(function ($rootScope, $compile) {
    element = angular.element('<esdms-upload></esdms-upload>');
    element = $compile(element)($rootScope);
    expect(element.text()).toBe('this is the esdmsUpload directive');
  }));*/
});
