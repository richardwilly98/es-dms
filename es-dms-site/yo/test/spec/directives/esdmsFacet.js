'use strict';

describe('Directive: esdmsFacet', function () {
  beforeEach(module('esDmsSiteApp'));

  /*var element;

  it('should make hidden element visible', inject(function ($rootScope, $compile) {
    element = angular.element('<esdms-facet></esdms-facet>');
    element = $compile(element)($rootScope);
    expect(element.text()).toBe('this is the esdmsFacet directive');
  }));*/
});
